#include <string>
#include <vector>
#include <sstream>

using namespace std;
class Util {
public:
    static vector<string> StringToVecByEnter(string request);
    static vector<string> StringToVecBySpace(string request);
};